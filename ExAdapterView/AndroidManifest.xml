<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="ks.appcenter.exadapterview"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".ExAdapterView"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
		<activity android:name=".ExListView"
                  android:label="@string/app_name"/>
        <activity android:name=".ExGridView"
                  android:label="@string/app_name"/>
        <activity android:name=".ExSpinner"
                  android:label="@string/app_name"/>
        <activity android:name=".ExGallery"
                  android:label="@string/app_name"/>
        <activity android:name=".CustomGrid"
                  android:label="@string/app_name"/>
        <activity android:name=".CustomList"
                  android:label="@string/app_name"/>
        <activity android:name=".ConnectedActivity"
                  android:label="@string/app_name"/>
    </application>
    <uses-sdk android:minSdkVersion="7" />

</manifest> 